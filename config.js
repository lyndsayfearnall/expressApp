const config = {
  host: "localhost",
  port: 8889,
  user: "root",
  password: "root",
  database: "db_cooperStuff"
};

module.exports = config; //exports it to the rest of the application, makes it a public file that node can read
